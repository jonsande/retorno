transit_sensor_pack
- Enables useful in-transit scanning (`scan`).
- Improves nearby signal detection without stopping travel.
- Not required for `nav map contacts` (that command depends on core_os).
