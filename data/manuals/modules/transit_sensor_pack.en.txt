MODULE DOSSIER — transit_sensor_pack

Role
- Sensor expansion package for useful scanning during transit.

Operational effect
- Improves near-signal acquisition while ship remains in transit posture.
- Extends practical reconnaissance without forcing travel interruption.

Integration notes
- Not a dependency for `nav map contacts` (that command is `core_os`-side).
