transit_sensor_pack
- Habilita barrido útil (`scan`) durante tránsito.
- Mejora detección de señales cercanas sin detener viaje.
- No es requisito para `nav map contacts` (ese comando depende de core_os).
