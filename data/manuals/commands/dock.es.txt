dock <node_id>
- Acopla la nave a un nodo.
- Requisitos:
- No debes estar en tránsito.
- Debes estar ya en el nodo objetivo (en órbita).
- El objetivo debe ser un contacto conocido.
- En algunos nodos no se permite el acoplamiento (p. ej., origen).
- El acoplamiento encola un trabajo; revisa con jobs.
- Para salir del acoplamiento y volver a órbita usa: undock
- El tiempo de dock se configura en `Balance.DOCK_TIME_S`.
- El dock puede interrumpirse si cambia el contexto antes de completar (por ejemplo, la nave entra en tránsito), o cancelarse con `job cancel <job_id>`.
