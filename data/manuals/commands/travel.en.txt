travel <node_id>
travel --no-cruise <node_id>
travel abort

Purpose
- Compatibility alias family for transit commands.

Command status
- Canonical command namespace: `nav`.

Equivalent forms
- `travel <node_id>` -> `nav <node_id>`
- `travel --no-cruise <node_id>` -> `nav --no-cruise <node_id>`
- `travel abort` -> `nav abort`

Related commands
- `nav <node_id>`, `nav abort`, `hibernate`
