cat <path>

Purpose
- Read file contents from ship filesystem mounts.

Operational behavior
- Uses localized variant automatically when available for current language.
- Returns text payload from file path if readable.

Usage notes
- Discover paths first with `ls`.
- Use absolute mount paths for deterministic access.

Related commands
- `ls [path]`, `mail read <id|latest>`, `man <topic>`
