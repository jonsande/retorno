route solve <node_id>
- Computes a travel route to a nearby known contact (within sensor range).
- Requires sensors >= limited and sensord running.
- Takes time; farther targets take longer.
- Blocked if power quality is too low (Q < 0.55) or brownout is active.
