log copy [n]

Purpose
- Copy recent operator/event log lines.

Operational behavior
- Copies last `n` lines (default bounded value) to clipboard/log sink.
- Designed for sharing diagnostics or preserving volatile session output.

Related commands
- `logs`, `alerts`, `jobs`
